# Task 0019: Fix stale text in pr-lifecycle.yml

Three text updates from PR #58 and #59 reviews:

1. Fix-request comment: "CRITICAL and WARNING items" → "review issues" (since both go through same loop now)
2. Merge-anyway log: "Merging with remaining warnings" → "Merging with remaining issues"  
3. Merge confirmation comment: "no critical/warning issues" → "Approved or issues addressed"

## Files
- `.github/workflows/pr-lifecycle.yml`
